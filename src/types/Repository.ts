export type Repository = {
  description: string;
  forksCount: number;
  htmlUrl: string;
  language: string;
  license: string;
  name: string;
  pushedAt: string;
  stargazersCount: number;
  id: number;
};

export type Repositories = Repository[];
